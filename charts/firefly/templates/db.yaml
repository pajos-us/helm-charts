apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: firefly-db
spec:
  instances: 1
  imageName: postgres:10-alpine3.16
  storage:
    size: 1Gi
    storageClass: pinas-nfs-client
  superuserSecret:
    name: firefly-db-superuser-secret
  bootstrap:
    initdb:
      secret:
        name: firefly-db-app-secret